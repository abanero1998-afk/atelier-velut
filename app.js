const cursor=document.getElementById("cursor");const cart=[];document.addEventListener("mousemove",e=>{if(!cursor)return;cursor.style.left=e.clientX+"px";cursor.style.top=e.clientY+"px"});document.querySelectorAll("a, button").forEach(el=>{el.addEventListener("mouseenter",()=>cursor&&cursor.classList.add("big"));el.addEventListener("mouseleave",()=>cursor&&cursor.classList.remove("big"))});document.querySelector("[data-open='menu']").onclick=()=>document.getElementById("menu").classList.add("open");document.querySelector("[data-close]").onclick=()=>document.getElementById("menu").classList.remove("open");document.getElementById("cartBtn").onclick=()=>document.getElementById("cart").classList.add("open");document.querySelector("[data-close-cart]").onclick=()=>document.getElementById("cart").classList.remove("open");document.querySelectorAll("#menu a").forEach(a=>a.addEventListener("click",()=>document.getElementById("menu").classList.remove("open")));function renderCart(){const box=document.getElementById("cartItems");const count=document.getElementById("cartCount");const total=document.getElementById("cartTotal");count.textContent=cart.length;if(!cart.length){box.innerHTML="<p class='note'>La borsa è vuota.</p>";total.textContent="€ 0";return}box.innerHTML=cart.map((i,idx)=>`<div class="cart-line"><span>${i.name}</span><span>€ ${i.price} <button data-rm="${idx}" style="background:none;border:0;color:#c4a574;cursor:pointer">rimuovi</button></span></div>`).join("");total.textContent="€ "+cart.reduce((s,i)=>s+i.price,0);box.querySelectorAll("[data-rm]").forEach(b=>{b.onclick=()=>{cart.splice(Number(b.dataset.rm),1);renderCart()}})}document.querySelectorAll(".add").forEach(btn=>{btn.onclick=()=>{cart.push({id:btn.dataset.id,name:btn.dataset.name,price:Number(btn.dataset.price)});renderCart();document.getElementById("cart").classList.add("open");btn.textContent="In borsa";setTimeout(()=>btn.textContent="Aggiungi alla borsa",1400)}});document.getElementById("checkout").onclick=()=>{if(!cart.length){document.getElementById("checkout").textContent="Aggiungi un capo prima";return}document.getElementById("checkout").textContent="Richiesta inviata — ti scriviamo"};document.getElementById("form").addEventListener("submit",e=>{e.preventDefault();document.getElementById("formOk").hidden=false;e.target.reset()});renderCart();